{
  "name": "exunit",
  "main": "./lib/exunit",
  "version": "1.0",
  "private": true,
  "description": "Atom ExUnit (Elixir) runner package",
  "activationCommands": {
    "atom-workspace": [
      "exunit:run",
      "exunit:run-for-line",
      "exunit:run-last",
      "exunit:run-all"
    ]
  },
  "repository": "https://github.com/axelson/atom-exunit",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "atom-space-pen-views": "^2.0.3"
  }
}
